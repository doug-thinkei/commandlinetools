#To show the whole history of all log information. 
git log --graph --oneline --all


#To remove all branches.
#Need to first checkout to 'development' branch. 

for i in `git branch | grep -v 'development'`; do git branch -D $i; done;


#Can I merge after this? 

git log --graph --decorate --oneline 

#Can I merge after this? 
